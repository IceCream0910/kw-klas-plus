<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KWQRCheckin" parent="Theme.Material3.Light.NoActionBar">

    </style>

    <style name="RoundCornerBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/RoundCornerModalStyle</item>
    </style>

    <style name="RoundCornerModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/white_round_top_border_20</item>
    </style>

    <style name="Theme.KWQRCheckin" parent="Base.Theme.KWQRCheckin" />

    <style name="Theme.KWQRCheckin.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <!-- Radius of the outer bound of widgets to make the rounded corners -->
        <item name="appWidgetRadius">16dp</item>
        <!--
        Radius of the inner view's bound of widgets to make the rounded corners.
        It needs to be 8dp or less than the value of appWidgetRadius
        -->
        <item name="appWidgetInnerRadius">8dp</item>
    </style>

    <style name="Theme.KWQRCheckin.AppWidgetContainer" parent="Theme.KWQRCheckin.AppWidgetContainerParent">
        <item name="appWidgetPadding">0dp</item>
    </style>
</resources>